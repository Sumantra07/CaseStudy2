Please use MongoD and Mongo commands to start the Mongo server and make the database ready before starting with this.

I have used post man tool to validate the REST API's ( as per the screenshots). NO UI is present.